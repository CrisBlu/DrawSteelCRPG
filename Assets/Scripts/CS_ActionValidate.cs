using UnityEngine;

public static class CS_ActionValidate //Check that action can be used
{
   /* public static bool UseMoveAction(TurnData turn)
    {

        if(turn.movement == turn.actor.Speed)
        {
            turn.moveAction = 0;
        }

        if(turn.moveAction == 0 && turn.movement == 0)
        {

        }

        return true;
    }*/
}
